[project]
name = "accounting-core"
version = "0.1.0"
description = "Shared money handling, domain models, and the tool registry that links accounting tools together."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/accounting_core"]
